#
# create a sheet
# 2 columns: “methods” and “success”
# 3rd column: “random”

# freeze top row
# input 50 “w” for wet in the methods column, and 50 “d” for dry in the methods column

# in the cell of the first row of the random column, input =RAND(), then drag down onto all other 99 rows from the corner of the first row cell.

# select all data cells (rows 1-100, all columns) using Cmd + A. then sort & filter, ascending by the values in the Random column
